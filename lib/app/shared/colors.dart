import 'package:flutter/material.dart';

class AppColors {
  // Text & Base
  static const Color mainBlack = Colors.black;
  static const Color mainWhite = Colors.white;

  // Background netral seperti kartu JKN
  static const Color secondaryWhite = Color(0xFFF8F8F8);

  // Warna utama (teal seperti ikon Mobile JKN)
  static const Color primary = Color(0xFF00A7A7);

  // Warna biru tombol “Ambil Antrian”
  static const Color primaryBlue = Color(0xFF3881C8);
  static const Color secondaryBlue = Color(0xFF07B9D8);

  // Warna header (biru muda – cyan)
  static const Color headerCyan = Color(0xFF27C7F7);

  // Kuning kecil untuk indikator tertentu (opsional)
  static const Color yellow = Color(0xFFFFC800);

  // Netral
  static const Color light = Color(0xFFF0F0F0);
  static const Color lightGrey = Color(0xFFBDBDBD);
  static const Color darkGrey = Color(0xFF5A5A5A);

  // Onboarding inactive (mengikuti tone cyan muda)
  static const Color onboardInactive = Color(0xFFD1F1F7);

  // Tidak digunakan di desain JKN, tetapi tetap disimpan
  static const Color calorie = Color(0xFFC19100);
  static const Color calorieBackgroundActive = Color(0xFFFADA7A);
  static const Color carbs = Color(0xFF9F5318);
  static const Color carbsBackgroundActive = Color(0xFF9F5318);
}
